<h1 class="text-center"> My restaurants </h1>
<h4 class="text-center">Admin</h4>

<div class="container">
  <div class="row">

    <% @restaurants.each do |restaurant| %>
      <li><%= link_to restaurant.name, admin_restaurant_path(restaurant) %> - <%= restaurant.address %> - <%= restaurant.category %></li>
      <% end %>
    </ul>

    <%= link_to "Add a new restaurant", new_admin_restaurant_path, class: "btn btn-primary" %>

  </div>
</div>
<ul>
